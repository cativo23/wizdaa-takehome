{
  "info": {
    "_postman_id": "8a3c2f10-7d9f-4f1b-9b5a-4b1e5c6a7d8e",
    "name": "Time-Off Microservice — Wizdaa Take-Home",
    "description": "# Time-Off Microservice\n\nA defensive time-off lifecycle service backed by NestJS + SQLite that syncs balances with an unreliable HCM source-of-truth over an at-least-once network. See `docs/TRD.md` for the full design (v3) and `docs/CONTRACTS.md` for the frozen interface spec.\n\n## How to use this collection\n\n1. Start the stack: `docker compose up --build` (app on `:3000`, mock HCM on `:3001`).\n2. Seed the mock HCM with a starting balance — **Mock HCM Controls → Refresh HCM balance** (default seeds `emp1/loc1 → 20` days).\n3. Walk through **Guided Scenarios** in order — each one references requests from folders 1–5.\n\n## Auth model\n\nThis service assumes an upstream gateway has authenticated the caller and injected:\n\n- `X-Employee-Id` — the principal's employee ID (trusted)\n- `X-Role` — `employee` or `manager`\n\nThe collection variables `employee_id`, `manager_id`, and `role` are wired into every header by default. Switch `role` to `manager` for manager-only routes (approve, reject, resolve-review).\n\n## Idempotency\n\n`POST /time-off-requests` requires an `Idempotency-Key` header (UUID). The collection pre-request script generates a fresh one for each submit. Replay the same submit with the same key to verify idempotent behavior (ADR-012).\n\n## State machine cheat-sheet\n\n```\nDRAFT ─submit─► PENDING ─approve─► PENDING_SYNC ─FILE ack─► APPROVED\n                  │                     │ └─retry cap─► REVERSE ► REJECTED\n                  │                     └─cancel─► CANCELLED (+ REVERSE if FILE sent)\n                  ├─reject───────────► REJECTED\n                  └─TTL (reaper)─────► EXPIRED\nAPPROVED ─cancel─► CANCELLED (+ REVERSE)\n```\n\nUnder the **Pure Outbox** model (ADR-013), `approve` always lands in `PENDING_SYNC` first; the dispatcher promotes to `APPROVED` on the FILE ack from HCM.",
    "schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json"
  },
  "item": [
    {
      "name": "1. Setup & Health",
      "description": "Sanity checks before walking through scenarios. Use these to confirm both services are up and reachable.",
      "item": [
        {
          "name": "App — Health (GET /)",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{base_url}}/",
              "host": ["{{base_url}}"],
              "path": [""]
            },
            "description": "Returns `{ status: 'ok' }` when the app is up. Used by the Docker healthcheck."
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('Status is 200', () => pm.response.to.have.status(200));",
                  "pm.test('status: ok', () => pm.expect(pm.response.json().status).to.eql('ok'));"
                ]
              }
            }
          ]
        },
        {
          "name": "Mock HCM — Ready check (GET /_control/batch)",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{hcm_url}}/_control/batch",
              "host": ["{{hcm_url}}"],
              "path": ["_control", "batch"]
            },
            "description": "Returns the batch corpus the mock HCM would emit next. Doubles as a liveness probe for the mock — also used by its Docker healthcheck."
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('Status is 200', () => pm.response.to.have.status(200));",
                  "pm.test('Corpus has sequence + asOf + balances', () => {",
                  "  const j = pm.response.json();",
                  "  pm.expect(j).to.have.property('sequence');",
                  "  pm.expect(j).to.have.property('asOf');",
                  "  pm.expect(j).to.have.property('balances');",
                  "});"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "2. Employee Workflow",
      "description": "All requests here use `X-Role: employee` and `X-Employee-Id: {{employee_id}}`. An employee may only act on their **own** records — IDOR is enforced server-side (§12).",
      "item": [
        {
          "name": "View own balance (GET /balances)",
          "request": {
            "method": "GET",
            "header": [
              {"key": "X-Employee-Id", "value": "{{employee_id}}"},
              {"key": "X-Role", "value": "employee"}
            ],
            "url": {
              "raw": "{{base_url}}/balances?employeeId={{employee_id}}&locationId={{location_id}}",
              "host": ["{{base_url}}"],
              "path": ["balances"],
              "query": [
                {"key": "employeeId", "value": "{{employee_id}}"},
                {"key": "locationId", "value": "{{location_id}}"}
              ]
            },
            "description": "Returns the current `Balance` row for `(employee_id, location_id)`. Creates a zero record on first access. Response fields: `available`, `reserved`, `needsReview`, `lastHcmAsOf`, `version`.\n\nUnder Model A: `available` mirrors HCM's free-days view; `reserved` = days held by PENDING requests."
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('Status is 200', () => pm.response.to.have.status(200));",
                  "pm.test('Has available/reserved fields', () => {",
                  "  const j = pm.response.json();",
                  "  pm.expect(j).to.have.property('available');",
                  "  pm.expect(j).to.have.property('reserved');",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "Submit time-off (POST /time-off-requests)",
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Generate a fresh Idempotency-Key for each submit (ADR-012).",
                  "// Re-running the SAME request without changing the key will replay (E8).",
                  "// Override by setting `idempotency_key` in collection variables to test replay.",
                  "if (!pm.collectionVariables.get('reuse_idempotency_key') || pm.collectionVariables.get('reuse_idempotency_key') === 'false') {",
                  "  const uuid = (typeof crypto !== 'undefined' && crypto.randomUUID)",
                  "    ? crypto.randomUUID()",
                  "    : 'idem-' + Date.now() + '-' + Math.random().toString(36).slice(2);",
                  "  pm.collectionVariables.set('idempotency_key', uuid);",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('Status is 201 or 200', () => pm.expect([200, 201]).to.include(pm.response.code));",
                  "pm.test('Response has id', () => {",
                  "  const j = pm.response.json();",
                  "  pm.expect(j.id).to.be.a('string');",
                  "  pm.collectionVariables.set('request_id', j.id);",
                  "});",
                  "pm.test('Status starts at PENDING', () => {",
                  "  pm.expect(pm.response.json().status).to.eql('PENDING');",
                  "});"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              {"key": "X-Employee-Id", "value": "{{employee_id}}"},
              {"key": "X-Role", "value": "employee"},
              {"key": "Idempotency-Key", "value": "{{idempotency_key}}"},
              {"key": "Content-Type", "value": "application/json"}
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"employeeId\": \"{{employee_id}}\",\n  \"locationId\": \"{{location_id}}\",\n  \"startDate\": \"2026-06-01\",\n  \"endDate\": \"2026-06-02\"\n}",
              "options": {"raw": {"language": "json"}}
            },
            "url": {
              "raw": "{{base_url}}/time-off-requests",
              "host": ["{{base_url}}"],
              "path": ["time-off-requests"]
            },
            "description": "Submit a time-off request (ADR-012). The server computes `days` from the inclusive date range, excluding weekends (location calendar is stubbed for v1 per A6).\n\n**Headers**\n- `X-Employee-Id` — must match the body's `employeeId` (IDOR check, §12)\n- `X-Role: employee`\n- `Idempotency-Key` — UUID; replays return the SAME request if active (E8); 422 on key reuse with a different body (E23)\n\n**Body**\n- `employeeId`, `locationId`, `startDate` (YYYY-MM-DD inclusive), `endDate` (YYYY-MM-DD inclusive)\n- **No `days` field** — server-computed (§12)\n\n**Responses**\n- `201` — request created in `PENDING`, `reserved += days` on the balance\n- `409` — insufficient balance OR overlap with an existing active request (E1, E19, E20)\n- `422` — idempotency-key reuse with a different body (E23)\n\nThe test script captures the new request `id` into `request_id` for subsequent approve/reject/cancel calls."
          }
        },
        {
          "name": "Cancel own request (POST /time-off-requests/:id/cancel)",
          "request": {
            "method": "POST",
            "header": [
              {"key": "X-Employee-Id", "value": "{{employee_id}}"},
              {"key": "X-Role", "value": "employee"},
              {"key": "Content-Type", "value": "application/json"}
            ],
            "body": {"mode": "raw", "raw": "{}", "options": {"raw": {"language": "json"}}},
            "url": {
              "raw": "{{base_url}}/time-off-requests/{{request_id}}/cancel",
              "host": ["{{base_url}}"],
              "path": ["time-off-requests", "{{request_id}}", "cancel"]
            },
            "description": "Cancel the captured request (`request_id`). Behavior depends on current status:\n\n- **PENDING** → void pending FILE outbox + `release` (reserved -= days) → `CANCELLED`\n- **PENDING_SYNC** (FILE not sent) → void FILE + `restore` (available += days) → `CANCELLED`\n- **PENDING_SYNC** (FILE already SENT) → `restore` + enqueue REVERSE → `CANCELLED` (E27)\n- **APPROVED** → `restore` + enqueue REVERSE → `CANCELLED` (E9, FR-6)\n- already **CANCELLED** → no-op (idempotent)\n\nA manager can cancel any request; an employee can only cancel their own (IDOR)."
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('Status is 200/201', () => pm.expect([200, 201]).to.include(pm.response.code));",
                  "pm.test('Final status is CANCELLED', () => pm.expect(pm.response.json().status).to.eql('CANCELLED'));"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "3. Manager Workflow",
      "description": "Manager-only routes: approve, reject, resolve-review. All use `X-Role: manager`. The `X-Employee-Id` header here is the **manager's** ID — IDOR for the target employee is handled inside the service.",
      "item": [
        {
          "name": "Approve request (POST /time-off-requests/:id/approve)",
          "request": {
            "method": "POST",
            "header": [
              {"key": "X-Employee-Id", "value": "{{manager_id}}"},
              {"key": "X-Role", "value": "manager"},
              {"key": "Content-Type", "value": "application/json"}
            ],
            "body": {"mode": "raw", "raw": "{}", "options": {"raw": {"language": "json"}}},
            "url": {
              "raw": "{{base_url}}/time-off-requests/{{request_id}}/approve",
              "host": ["{{base_url}}"],
              "path": ["time-off-requests", "{{request_id}}", "approve"]
            },
            "description": "Approve a PENDING request (ADR-001).\n\n**Approve sequence** under the **Pure Outbox** model (ADR-013):\n1. GET HCM realtime balance (best-effort; if HCM is unreachable → proceed on local cache per ADR-001).\n2. `applyHcmSnapshot` — set local `available = hcmValue`.\n3. Re-validate locally: if `available < days` → REJECTED + `release` (E10).\n4. Past-date check — if `startDate < now` (location-tz civil date) → REJECTED + `release` (E18).\n5. In one transaction: `commit` (available -= days, reserved -= days), write `Outbox(FILE, PENDING)`, status → **PENDING_SYNC**.\n\nThe dispatcher promotes `PENDING_SYNC → APPROVED` once HCM acks the FILE (E4 happy). On retry-cap, the dispatcher fires the compensating REVERSE path (E5)."
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('Status code is 200/201', () => pm.expect([200, 201]).to.include(pm.response.code));",
                  "pm.test('Request transitioned to PENDING_SYNC (Pure Outbox)', () => {",
                  "  const j = pm.response.json();",
                  "  // Happy path under Pure Outbox: approve always lands in PENDING_SYNC;",
                  "  // dispatcher (5s @Interval) promotes to APPROVED on FILE ack.",
                  "  pm.expect(['PENDING_SYNC', 'APPROVED', 'REJECTED']).to.include(j.status);",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "Reject request (POST /time-off-requests/:id/reject)",
          "request": {
            "method": "POST",
            "header": [
              {"key": "X-Employee-Id", "value": "{{manager_id}}"},
              {"key": "X-Role", "value": "manager"},
              {"key": "Content-Type", "value": "application/json"}
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"reason\": \"Conflicts with team coverage window\"\n}",
              "options": {"raw": {"language": "json"}}
            },
            "url": {
              "raw": "{{base_url}}/time-off-requests/{{request_id}}/reject",
              "host": ["{{base_url}}"],
              "path": ["time-off-requests", "{{request_id}}", "reject"]
            },
            "description": "Reject a PENDING or PENDING_SYNC request.\n\n- **PENDING** → `release` (reserved -= days) → REJECTED\n- **PENDING_SYNC** → void pending FILE + `restore` (available += days, the commit is undone) → REJECTED\n\n`reason` is accepted but not persisted in v1 (extensibility seam)."
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('Status code is 200/201', () => pm.expect([200, 201]).to.include(pm.response.code));",
                  "pm.test('Final status is REJECTED', () => pm.expect(pm.response.json().status).to.eql('REJECTED'));"
                ]
              }
            }
          ]
        },
        {
          "name": "Resolve review (PATCH /balances/resolve-review)",
          "request": {
            "method": "PATCH",
            "header": [
              {"key": "X-Employee-Id", "value": "{{manager_id}}"},
              {"key": "X-Role", "value": "manager"},
              {"key": "Content-Type", "value": "application/json"}
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"employeeId\": \"{{employee_id}}\",\n  \"locationId\": \"{{location_id}}\"\n}",
              "options": {"raw": {"language": "json"}}
            },
            "url": {
              "raw": "{{base_url}}/balances/resolve-review",
              "host": ["{{base_url}}"],
              "path": ["balances", "resolve-review"]
            },
            "description": "Clear `Balance.needsReview` on a flagged balance (B4 / ADR-003).\n\nWhen reconciliation drives `available` negative, the balance is flagged with `needsReview=true` and a `FLAGGED_NEGATIVE` event is emitted (E26). After the manager investigates and accepts the divergence (or manually adjusts), they call this endpoint to clear the flag.\n\nManager-only. This is the ONE BalanceService method that acquires the per-balance-key lock itself."
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('Status code is 200/204', () => pm.expect([200, 204]).to.include(pm.response.code));"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "4. HCM Batch Sync",
      "description": "The HCM → service push (§8). HCM emits a full balance corpus periodically (or on events like year-start); the service reconciles each balance against the snapshot under ADR-003's algorithm.",
      "item": [
        {
          "name": "Ingest HCM batch corpus (POST /timeoff/hcm/batch)",
          "request": {
            "method": "POST",
            "header": [
              {"key": "Content-Type", "value": "application/json"}
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"sequence\": 1,\n  \"asOf\": \"2026-06-15T00:00:00.000Z\",\n  \"balances\": [\n    {\n      \"employeeId\": \"{{employee_id}}\",\n      \"locationId\": \"{{location_id}}\",\n      \"balance\": 18,\n      \"asOf\": \"2026-06-15T00:00:00.000Z\"\n    }\n  ]\n}",
              "options": {"raw": {"language": "json"}}
            },
            "url": {
              "raw": "{{base_url}}/timeoff/hcm/batch",
              "host": ["{{base_url}}"],
              "path": ["timeoff", "hcm", "batch"]
            },
            "description": "Ingest a batch corpus from HCM (ADR-009 + ADR-003).\n\n**Sequence guard (ADR-009):** `sequence` must be strictly greater than the last applied sequence. A stale or out-of-order batch is rejected and audited with a single `STALE_REJECTED` reconciliation event (E13).\n\n**Per-balance reconcile (ADR-003):** for each entry, under the per-balance-key lock:\n- base `available = entry.balance`\n- subtract days for any `APPROVED` or `PENDING_SYNC` request whose `hcmAckAt` is NULL or > `asOf` (snapshot misses these)\n- add days for any pending REVERSE outbox row whose linked request is `CANCELLED`\n- `reserved` recomputed from current `PENDING` requests only\n- if final `available < 0` → set `needsReview = true` + emit `FLAGGED_NEGATIVE` (E26)\n\nIn production this endpoint would require a service token. In v1 it's open by design (commented in the controller).\n\n**Bump `sequence` between runs!** The service rejects any sequence ≤ the last applied one."
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('Status code is 200/201/202', () => pm.expect([200, 201, 202]).to.include(pm.response.code));"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "5. Mock HCM Controls",
      "description": "Test-control surface on the mock HCM (`:3001`). These endpoints exist ONLY in the mock — in a real deployment the `mock-hcm` service is dropped and the real HCM has no `_control/*` routes (§12).\n\nUse these to:\n- Set HCM-side starting balance (`/_control/refresh`)\n- Switch behavior between scenarios (`/_control/scenario`)\n- Preview or emit a batch corpus (`/_control/batch`, `/_control/emit-batch`)\n- Directly inspect HCM-side state (`GET /hcm/balance`)",
      "item": [
        {
          "name": "Seed / refresh HCM balance (POST /_control/refresh)",
          "request": {
            "method": "POST",
            "header": [{"key": "Content-Type", "value": "application/json"}],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"employeeId\": \"{{employee_id}}\",\n  \"locationId\": \"{{location_id}}\",\n  \"balance\": 20\n}",
              "options": {"raw": {"language": "json"}}
            },
            "url": {
              "raw": "{{hcm_url}}/_control/refresh",
              "host": ["{{hcm_url}}"],
              "path": ["_control", "refresh"]
            },
            "description": "Overwrite an HCM-side balance with a new value (and refresh `asOf` to now). Simulates a year-start refresh or work-anniversary bonus, making E7 / E10 / E26 scenarios deterministic.\n\nRun this once at the start of a session to seed a known balance, e.g. `{ balance: 20 }`."
          }
        },
        {
          "name": "Set scenario (POST /_control/scenario)",
          "request": {
            "method": "POST",
            "header": [{"key": "Content-Type", "value": "application/json"}],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"scenario\": \"correct\"\n}",
              "options": {"raw": {"language": "json"}}
            },
            "url": {
              "raw": "{{hcm_url}}/_control/scenario",
              "host": ["{{hcm_url}}"],
              "path": ["_control", "scenario"]
            },
            "description": "Switch the mock's behavior. ADR-007 scenarios:\n\n| Scenario | Effect |\n|---|---|\n| `correct` | Normal semantics; idempotency dedup applies. |\n| `silent-insufficient` | `POST /hcm/timeoff` returns 200 even on insufficient balance, WITHOUT deducting (E3). |\n| `timeout` | All `/hcm/*` calls fail (503) — exercises the dispatcher retry / retry-cap path. |\n| `mutate-between-calls` | Each `GET /hcm/balance` decrements by 1 — exercises E10. |\n| `divergent-batch` | Batch corpus reports each balance 3 below the real value (E6). |\n| `duplicate-delivery` | Behaves like `correct` (idempotency-key dedup still holds; E11/E16). |\n| `ignore-idempotency-key` | Skips dedup table — proves the service's local guard holds even when HCM ignores ADR-008. |"
          }
        },
        {
          "name": "Preview batch corpus (GET /_control/batch)",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{hcm_url}}/_control/batch",
              "host": ["{{hcm_url}}"],
              "path": ["_control", "batch"]
            },
            "description": "Return the corpus the mock WOULD emit next. Non-destructive (does NOT advance the sequence counter). Useful for inspecting state before triggering a real ingest."
          }
        },
        {
          "name": "Emit batch to service (POST /_control/emit-batch)",
          "request": {
            "method": "POST",
            "header": [{"key": "Content-Type", "value": "application/json"}],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"targetUrl\": \"http://app:3000/timeoff/hcm/batch\"\n}",
              "options": {"raw": {"language": "json"}}
            },
            "url": {
              "raw": "{{hcm_url}}/_control/emit-batch",
              "host": ["{{hcm_url}}"],
              "path": ["_control", "emit-batch"]
            },
            "description": "Build a fresh corpus (advancing the sequence counter) and POST it to `targetUrl`. The default uses the Docker network hostname `app:3000` — if you're calling Postman from outside Docker, swap it for `http://host.docker.internal:3000` or `http://localhost:3000` depending on how your Docker daemon resolves the host.\n\nOptional overrides: `balances`, `asOf`, `sequence`."
          }
        },
        {
          "name": "Direct: get HCM balance (GET /hcm/balance)",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{hcm_url}}/hcm/balance?employeeId={{employee_id}}&locationId={{location_id}}",
              "host": ["{{hcm_url}}"],
              "path": ["hcm", "balance"],
              "query": [
                {"key": "employeeId", "value": "{{employee_id}}"},
                {"key": "locationId", "value": "{{location_id}}"}
              ]
            },
            "description": "Inspect the HCM-side balance directly (debugging / cross-checking). The service never calls this from Postman — the HcmClient does — but it's useful to verify HCM-side state after a FILE or REVERSE has landed."
          }
        }
      ]
    },
    {
      "name": "6. Guided Scenarios",
      "description": "Runnable end-to-end scenario folders. Each sub-folder maps to one E-matrix flow. Select a folder and use **Run folder** (Collection Runner) to execute every step in order.\n\nReset between scenarios: run **Mock HCM Controls → Seed / refresh HCM balance** with your starting value. For a completely clean DB use `docker compose down -v && docker compose up -d`.",
      "item": [
        {
          "name": "A — Happy path (submit → approve → APPROVED)",
          "description": "## Goal\nWalk the canonical lifecycle through the Pure Outbox dispatcher: seed HCM → lazy-hydrate balance → submit → approve → wait for dispatcher → verify APPROVED and balance deducted.\n\n## Expected flow\n1. Seed HCM to 20 days.\n2. Set scenario to `correct`.\n3. View balance — cold read triggers lazy-hydrate (ADR-014); `available=20` returned.\n4. Submit 2 business days (Jun 1–2) → PENDING, `reserved=2`.\n5. View balance → `available=20, reserved=2`.\n6. Approve → PENDING_SYNC (Pure Outbox: dispatcher fires every 5 s).\n7. Wait 6 s for dispatcher tick.\n8. View balance → `available=18, reserved=0` (FILE acked, deduction committed).\n9. Check HCM directly → `balance=18`.\n\n## What to verify\n- Step 4: status=PENDING, id saved to `request_id`.\n- Step 6: status in [PENDING_SYNC, APPROVED].\n- Step 8: available=18, reserved=0.\n- Step 9: HCM-side balance=18.",
          "item": [
            {
              "name": "A1 — Seed HCM balance (20)",
              "request": {
                "method": "POST",
                "header": [{"key": "Content-Type", "value": "application/json"}],
                "body": {
                  "mode": "raw",
                  "raw": "{\n  \"employeeId\": \"{{employee_id}}\",\n  \"locationId\": \"{{location_id}}\",\n  \"balance\": 20\n}",
                  "options": {"raw": {"language": "json"}}
                },
                "url": {
                  "raw": "{{hcm_url}}/_control/refresh",
                  "host": ["{{hcm_url}}"],
                  "path": ["_control", "refresh"]
                },
                "description": "Seed HCM-side balance to 20 for a clean starting state."
              },
              "event": [
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": ["pm.test('Seed OK', () => pm.response.to.have.status(200));"]
                  }
                }
              ]
            },
            {
              "name": "A2 — Set scenario: correct",
              "request": {
                "method": "POST",
                "header": [{"key": "Content-Type", "value": "application/json"}],
                "body": {
                  "mode": "raw",
                  "raw": "{\n  \"scenario\": \"correct\"\n}",
                  "options": {"raw": {"language": "json"}}
                },
                "url": {
                  "raw": "{{hcm_url}}/_control/scenario",
                  "host": ["{{hcm_url}}"],
                  "path": ["_control", "scenario"]
                },
                "description": "Switch mock HCM to normal correct behavior."
              },
              "event": [
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": ["pm.test('Scenario set OK', () => pm.response.to.have.status(200));"]
                  }
                }
              ]
            },
            {
              "name": "A3 — View balance (cold-read lazy-hydrate)",
              "request": {
                "method": "GET",
                "header": [
                  {"key": "X-Employee-Id", "value": "{{employee_id}}"},
                  {"key": "X-Role", "value": "employee"}
                ],
                "url": {
                  "raw": "{{base_url}}/balances?employeeId={{employee_id}}&locationId={{location_id}}",
                  "host": ["{{base_url}}"],
                  "path": ["balances"],
                  "query": [
                    {"key": "employeeId", "value": "{{employee_id}}"},
                    {"key": "locationId", "value": "{{location_id}}"}
                  ]
                },
                "description": "First read on a cold (lastHcmAsOf=null) tuple triggers ADR-014 lazy-hydration from HCM. Assert available=20 and lastHcmAsOf is present."
              },
              "event": [
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Status 200', () => pm.response.to.have.status(200));",
                      "pm.test('available=20 after lazy-hydrate', () => {",
                      "  const j = pm.response.json();",
                      "  pm.expect(j.available).to.eql(20);",
                      "});",
                      "pm.test('lastHcmAsOf is present', () => {",
                      "  pm.expect(pm.response.json().lastHcmAsOf).to.be.a('string');",
                      "});"
                    ]
                  }
                }
              ]
            },
            {
              "name": "A4 — Submit time-off (Jun 1–2, 2 days)",
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const uuid = (typeof crypto !== 'undefined' && crypto.randomUUID)",
                      "  ? crypto.randomUUID()",
                      "  : 'idem-' + Date.now() + '-' + Math.random().toString(36).slice(2);",
                      "pm.collectionVariables.set('idempotency_key', uuid);"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Status 201', () => pm.response.to.have.status(201));",
                      "pm.test('Status=PENDING, id saved', () => {",
                      "  const j = pm.response.json();",
                      "  pm.expect(j.status).to.eql('PENDING');",
                      "  pm.expect(j.id).to.be.a('string');",
                      "  pm.collectionVariables.set('request_id', j.id);",
                      "});"
                    ]
                  }
                }
              ],
              "request": {
                "method": "POST",
                "header": [
                  {"key": "X-Employee-Id", "value": "{{employee_id}}"},
                  {"key": "X-Role", "value": "employee"},
                  {"key": "Idempotency-Key", "value": "{{idempotency_key}}"},
                  {"key": "Content-Type", "value": "application/json"}
                ],
                "body": {
                  "mode": "raw",
                  "raw": "{\n  \"employeeId\": \"{{employee_id}}\",\n  \"locationId\": \"{{location_id}}\",\n  \"startDate\": \"2026-06-01\",\n  \"endDate\": \"2026-06-02\"\n}",
                  "options": {"raw": {"language": "json"}}
                },
                "url": {
                  "raw": "{{base_url}}/time-off-requests",
                  "host": ["{{base_url}}"],
                  "path": ["time-off-requests"]
                },
                "description": "Submit 2 business days. Expect 201, status=PENDING, request_id captured."
              }
            },
            {
              "name": "A5 — View balance (reserved=2)",
              "request": {
                "method": "GET",
                "header": [
                  {"key": "X-Employee-Id", "value": "{{employee_id}}"},
                  {"key": "X-Role", "value": "employee"}
                ],
                "url": {
                  "raw": "{{base_url}}/balances?employeeId={{employee_id}}&locationId={{location_id}}",
                  "host": ["{{base_url}}"],
                  "path": ["balances"],
                  "query": [
                    {"key": "employeeId", "value": "{{employee_id}}"},
                    {"key": "locationId", "value": "{{location_id}}"}
                  ]
                },
                "description": "After submit: available=20 (unchanged), reserved=2 (days held). Model A: available is decremented at commit, not at reserve."
              },
              "event": [
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Status 200', () => pm.response.to.have.status(200));",
                      "pm.test('available=20, reserved=2', () => {",
                      "  const j = pm.response.json();",
                      "  pm.expect(j.available).to.eql(20);",
                      "  pm.expect(j.reserved).to.eql(2);",
                      "});"
                    ]
                  }
                }
              ]
            },
            {
              "name": "A6 — Approve request",
              "request": {
                "method": "POST",
                "header": [
                  {"key": "X-Employee-Id", "value": "{{manager_id}}"},
                  {"key": "X-Role", "value": "manager"},
                  {"key": "Content-Type", "value": "application/json"}
                ],
                "body": {"mode": "raw", "raw": "{}", "options": {"raw": {"language": "json"}}},
                "url": {
                  "raw": "{{base_url}}/time-off-requests/{{request_id}}/approve",
                  "host": ["{{base_url}}"],
                  "path": ["time-off-requests", "{{request_id}}", "approve"]
                },
                "description": "Manager approves. Under Pure Outbox, status lands in PENDING_SYNC. Dispatcher promotes to APPROVED on FILE ack (~5 s)."
              },
              "event": [
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Status 200/201', () => pm.expect([200, 201]).to.include(pm.response.code));",
                      "pm.test('Status is PENDING_SYNC or APPROVED', () => {",
                      "  pm.expect(['PENDING_SYNC', 'APPROVED']).to.include(pm.response.json().status);",
                      "});"
                    ]
                  }
                }
              ]
            },
            {
              "name": "⏱ Wait 6s (dispatcher tick)",
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "// Pause 6 seconds so the @Interval(5000) dispatcher has time to fire and ack the FILE.",
                      "const now = Date.now();",
                      "while (Date.now() - now < 6000) { /* busy-wait */ }"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": ["pm.test('Wait complete', () => pm.response.to.have.status(200));"]
                  }
                }
              ],
              "request": {
                "method": "GET",
                "header": [],
                "url": {
                  "raw": "{{base_url}}/",
                  "host": ["{{base_url}}"],
                  "path": [""]
                },
                "description": "Busy-wait 6 s in the pre-request script so the dispatcher @Interval(5000) has fired at least once and promoted the outbox FILE."
              }
            },
            {
              "name": "A7 — View balance (available=18, reserved=0)",
              "request": {
                "method": "GET",
                "header": [
                  {"key": "X-Employee-Id", "value": "{{employee_id}}"},
                  {"key": "X-Role", "value": "employee"}
                ],
                "url": {
                  "raw": "{{base_url}}/balances?employeeId={{employee_id}}&locationId={{location_id}}",
                  "host": ["{{base_url}}"],
                  "path": ["balances"],
                  "query": [
                    {"key": "employeeId", "value": "{{employee_id}}"},
                    {"key": "locationId", "value": "{{location_id}}"}
                  ]
                },
                "description": "Post-approval: commit deducted available by 2 and cleared the reservation. Expect available=18, reserved=0."
              },
              "event": [
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Status 200', () => pm.response.to.have.status(200));",
                      "pm.test('available=18, reserved=0', () => {",
                      "  const j = pm.response.json();",
                      "  pm.expect(j.available).to.eql(18);",
                      "  pm.expect(j.reserved).to.eql(0);",
                      "});"
                    ]
                  }
                }
              ]
            },
            {
              "name": "A8 — Direct: verify HCM balance=18",
              "request": {
                "method": "GET",
                "header": [],
                "url": {
                  "raw": "{{hcm_url}}/hcm/balance?employeeId={{employee_id}}&locationId={{location_id}}",
                  "host": ["{{hcm_url}}"],
                  "path": ["hcm", "balance"],
                  "query": [
                    {"key": "employeeId", "value": "{{employee_id}}"},
                    {"key": "locationId", "value": "{{location_id}}"}
                  ]
                },
                "description": "Cross-check HCM side: the FILE landed and deducted 2 days. Expect balance=18."
              },
              "event": [
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Status 200', () => pm.response.to.have.status(200));",
                      "pm.test('HCM balance=18', () => {",
                      "  pm.expect(pm.response.json().balance).to.eql(18);",
                      "});"
                    ]
                  }
                }
              ]
            }
          ]
        },
        {
          "name": "B — Insufficient balance at submit (E1)",
          "description": "## Goal\nVerify the local guard rejects a submit that would exceed available balance (E1, ADR-001).\n\n## Expected flow\n1. Seed HCM to 2 days and set scenario correct.\n2. View balance → lazy-hydrate to available=2.\n3. Submit 22 business days (Jun 1–Jun 30) → 409 Conflict.\n\n## What to verify\n- Step 3: 409 with a message about insufficient balance.\n- No reservation created: view balance after 409 still shows reserved=0.",
          "item": [
            {
              "name": "B1 — Seed HCM balance (2)",
              "request": {
                "method": "POST",
                "header": [{"key": "Content-Type", "value": "application/json"}],
                "body": {
                  "mode": "raw",
                  "raw": "{\n  \"employeeId\": \"{{employee_id}}\",\n  \"locationId\": \"{{location_id}}\",\n  \"balance\": 2\n}",
                  "options": {"raw": {"language": "json"}}
                },
                "url": {
                  "raw": "{{hcm_url}}/_control/refresh",
                  "host": ["{{hcm_url}}"],
                  "path": ["_control", "refresh"]
                },
                "description": "Seed HCM to only 2 days — insufficient for a long request."
              },
              "event": [
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": ["pm.test('Seed OK', () => pm.response.to.have.status(200));"]
                  }
                }
              ]
            },
            {
              "name": "B2 — Set scenario: correct",
              "request": {
                "method": "POST",
                "header": [{"key": "Content-Type", "value": "application/json"}],
                "body": {
                  "mode": "raw",
                  "raw": "{\n  \"scenario\": \"correct\"\n}",
                  "options": {"raw": {"language": "json"}}
                },
                "url": {
                  "raw": "{{hcm_url}}/_control/scenario",
                  "host": ["{{hcm_url}}"],
                  "path": ["_control", "scenario"]
                },
                "description": "Normal HCM behavior."
              },
              "event": [
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": ["pm.test('Scenario set OK', () => pm.response.to.have.status(200));"]
                  }
                }
              ]
            },
            {
              "name": "B3 — View balance (lazy-hydrate to 2)",
              "request": {
                "method": "GET",
                "header": [
                  {"key": "X-Employee-Id", "value": "{{employee_id}}"},
                  {"key": "X-Role", "value": "employee"}
                ],
                "url": {
                  "raw": "{{base_url}}/balances?employeeId={{employee_id}}&locationId={{location_id}}",
                  "host": ["{{base_url}}"],
                  "path": ["balances"],
                  "query": [
                    {"key": "employeeId", "value": "{{employee_id}}"},
                    {"key": "locationId", "value": "{{location_id}}"}
                  ]
                },
                "description": "Cold-read forces lazy-hydrate; available should reflect 2."
              },
              "event": [
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Status 200', () => pm.response.to.have.status(200));",
                      "pm.test('available=2 after hydration', () => {",
                      "  pm.expect(pm.response.json().available).to.eql(2);",
                      "});"
                    ]
                  }
                }
              ]
            },
            {
              "name": "B4 — Submit 22 days → 409 Conflict (E1)",
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const uuid = (typeof crypto !== 'undefined' && crypto.randomUUID)",
                      "  ? crypto.randomUUID()",
                      "  : 'idem-' + Date.now() + '-' + Math.random().toString(36).slice(2);",
                      "pm.collectionVariables.set('idempotency_key', uuid);"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('409 Conflict — insufficient balance', () => pm.response.to.have.status(409));"
                    ]
                  }
                }
              ],
              "request": {
                "method": "POST",
                "header": [
                  {"key": "X-Employee-Id", "value": "{{employee_id}}"},
                  {"key": "X-Role", "value": "employee"},
                  {"key": "Idempotency-Key", "value": "{{idempotency_key}}"},
                  {"key": "Content-Type", "value": "application/json"}
                ],
                "body": {
                  "mode": "raw",
                  "raw": "{\n  \"employeeId\": \"{{employee_id}}\",\n  \"locationId\": \"{{location_id}}\",\n  \"startDate\": \"2026-06-01\",\n  \"endDate\": \"2026-06-30\"\n}",
                  "options": {"raw": {"language": "json"}}
                },
                "url": {
                  "raw": "{{base_url}}/time-off-requests",
                  "host": ["{{base_url}}"],
                  "path": ["time-off-requests"]
                },
                "description": "Jun 1–30 spans ~22 business days, far exceeding available=2. Expect 409 ConflictException from the local guard (ADR-001/E1)."
              }
            },
            {
              "name": "B5 — View balance (no reservation created)",
              "request": {
                "method": "GET",
                "header": [
                  {"key": "X-Employee-Id", "value": "{{employee_id}}"},
                  {"key": "X-Role", "value": "employee"}
                ],
                "url": {
                  "raw": "{{base_url}}/balances?employeeId={{employee_id}}&locationId={{location_id}}",
                  "host": ["{{base_url}}"],
                  "path": ["balances"],
                  "query": [
                    {"key": "employeeId", "value": "{{employee_id}}"},
                    {"key": "locationId", "value": "{{location_id}}"}
                  ]
                },
                "description": "Guard fired before any reservation — reserved must still be 0."
              },
              "event": [
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Status 200', () => pm.response.to.have.status(200));",
                      "pm.test('reserved=0 — no reservation created', () => {",
                      "  pm.expect(pm.response.json().reserved).to.eql(0);",
                      "});"
                    ]
                  }
                }
              ]
            }
          ]
        },
        {
          "name": "C — Idempotent replay (E8)",
          "description": "## Goal\nProve that submitting twice with the same `Idempotency-Key` returns the same request ID and does NOT double the reservation (E8, ADR-012 guard ①).\n\n## Expected flow\n1. Seed balance=20, scenario correct, view balance.\n2. Submit with fixed key `e8-replay-key` → PENDING, id=X, reserved=2.\n3. Submit again with the same fixed key and identical body → same id=X returned, reserved still =2.\n4. View balance → reserved=2, not 4.\n\n## What to verify\n- Step 3: response id matches step 2 id.\n- Step 4: reserved=2 (single reservation).",
          "item": [
            {
              "name": "C1 — Seed HCM balance (20)",
              "request": {
                "method": "POST",
                "header": [{"key": "Content-Type", "value": "application/json"}],
                "body": {
                  "mode": "raw",
                  "raw": "{\n  \"employeeId\": \"{{employee_id}}\",\n  \"locationId\": \"{{location_id}}\",\n  \"balance\": 20\n}",
                  "options": {"raw": {"language": "json"}}
                },
                "url": {
                  "raw": "{{hcm_url}}/_control/refresh",
                  "host": ["{{hcm_url}}"],
                  "path": ["_control", "refresh"]
                },
                "description": "Seed to 20."
              },
              "event": [
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": ["pm.test('Seed OK', () => pm.response.to.have.status(200));"]
                  }
                }
              ]
            },
            {
              "name": "C2 — Set scenario: correct",
              "request": {
                "method": "POST",
                "header": [{"key": "Content-Type", "value": "application/json"}],
                "body": {
                  "mode": "raw",
                  "raw": "{\n  \"scenario\": \"correct\"\n}",
                  "options": {"raw": {"language": "json"}}
                },
                "url": {
                  "raw": "{{hcm_url}}/_control/scenario",
                  "host": ["{{hcm_url}}"],
                  "path": ["_control", "scenario"]
                },
                "description": "Normal HCM behavior."
              },
              "event": [
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": ["pm.test('Scenario set OK', () => pm.response.to.have.status(200));"]
                  }
                }
              ]
            },
            {
              "name": "C3 — View balance (lazy-hydrate)",
              "request": {
                "method": "GET",
                "header": [
                  {"key": "X-Employee-Id", "value": "{{employee_id}}"},
                  {"key": "X-Role", "value": "employee"}
                ],
                "url": {
                  "raw": "{{base_url}}/balances?employeeId={{employee_id}}&locationId={{location_id}}",
                  "host": ["{{base_url}}"],
                  "path": ["balances"],
                  "query": [
                    {"key": "employeeId", "value": "{{employee_id}}"},
                    {"key": "locationId", "value": "{{location_id}}"}
                  ]
                },
                "description": "Warm up the balance row via lazy-hydrate."
              },
              "event": [
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": ["pm.test('Status 200', () => pm.response.to.have.status(200));"]
                  }
                }
              ]
            },
            {
              "name": "C4 — Submit #1 (fixed key e8-replay-key)",
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "// Use a fixed replay key for this scenario.",
                      "pm.collectionVariables.set('idempotency_key_replay', 'e8-replay-key-' + pm.collectionVariables.get('employee_id'));"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Status 201', () => pm.response.to.have.status(201));",
                      "pm.test('PENDING, id saved', () => {",
                      "  const j = pm.response.json();",
                      "  pm.expect(j.status).to.eql('PENDING');",
                      "  pm.collectionVariables.set('request_id', j.id);",
                      "});"
                    ]
                  }
                }
              ],
              "request": {
                "method": "POST",
                "header": [
                  {"key": "X-Employee-Id", "value": "{{employee_id}}"},
                  {"key": "X-Role", "value": "employee"},
                  {"key": "Idempotency-Key", "value": "{{idempotency_key_replay}}"},
                  {"key": "Content-Type", "value": "application/json"}
                ],
                "body": {
                  "mode": "raw",
                  "raw": "{\n  \"employeeId\": \"{{employee_id}}\",\n  \"locationId\": \"{{location_id}}\",\n  \"startDate\": \"2026-06-01\",\n  \"endDate\": \"2026-06-02\"\n}",
                  "options": {"raw": {"language": "json"}}
                },
                "url": {
                  "raw": "{{base_url}}/time-off-requests",
                  "host": ["{{base_url}}"],
                  "path": ["time-off-requests"]
                },
                "description": "First submit with fixed key. Captures id1 into request_id."
              }
            },
            {
              "name": "C5 — Submit #2 (same key, same body) → same id returned",
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "// Ensure the same fixed key is still set (no re-generation).",
                      "pm.collectionVariables.set('idempotency_key_replay', 'e8-replay-key-' + pm.collectionVariables.get('employee_id'));"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Status 200 or 201 (idempotent replay)', () => pm.expect([200, 201]).to.include(pm.response.code));",
                      "pm.test('Same id returned as first submit', () => {",
                      "  pm.expect(pm.response.json().id).to.eql(pm.collectionVariables.get('request_id'));",
                      "});"
                    ]
                  }
                }
              ],
              "request": {
                "method": "POST",
                "header": [
                  {"key": "X-Employee-Id", "value": "{{employee_id}}"},
                  {"key": "X-Role", "value": "employee"},
                  {"key": "Idempotency-Key", "value": "{{idempotency_key_replay}}"},
                  {"key": "Content-Type", "value": "application/json"}
                ],
                "body": {
                  "mode": "raw",
                  "raw": "{\n  \"employeeId\": \"{{employee_id}}\",\n  \"locationId\": \"{{location_id}}\",\n  \"startDate\": \"2026-06-01\",\n  \"endDate\": \"2026-06-02\"\n}",
                  "options": {"raw": {"language": "json"}}
                },
                "url": {
                  "raw": "{{base_url}}/time-off-requests",
                  "host": ["{{base_url}}"],
                  "path": ["time-off-requests"]
                },
                "description": "Exact replay of C4 — same key, same body. Service must return the existing request (same id), not create a second one."
              }
            },
            {
              "name": "C6 — View balance (reserved=2, not 4)",
              "request": {
                "method": "GET",
                "header": [
                  {"key": "X-Employee-Id", "value": "{{employee_id}}"},
                  {"key": "X-Role", "value": "employee"}
                ],
                "url": {
                  "raw": "{{base_url}}/balances?employeeId={{employee_id}}&locationId={{location_id}}",
                  "host": ["{{base_url}}"],
                  "path": ["balances"],
                  "query": [
                    {"key": "employeeId", "value": "{{employee_id}}"},
                    {"key": "locationId", "value": "{{location_id}}"}
                  ]
                },
                "description": "Idempotent replay must NOT double the reservation. Assert reserved=2."
              },
              "event": [
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Status 200', () => pm.response.to.have.status(200));",
                      "pm.test('reserved=2 — single reservation only', () => {",
                      "  pm.expect(pm.response.json().reserved).to.eql(2);",
                      "});"
                    ]
                  }
                }
              ]
            }
          ]
        },
        {
          "name": "D — Boundary-touching overlap (E20)",
          "description": "## Goal\nVerify the inclusive overlap predicate rejects a second submit whose date range shares exactly one boundary day with an existing active request (E20, ADR-012 guard ②).\n\n## Expected flow\n1. Seed balance=20, scenario correct, view balance.\n2. Submit Jun 2–3 (key D1) → PENDING.\n3. Submit Jun 3–4 (key D2, new key) → 409 Conflict (shared Jun 3).\n\n## What to verify\n- Step 3: 409, not a new PENDING request.",
          "item": [
            {
              "name": "D1 — Seed HCM balance (20)",
              "request": {
                "method": "POST",
                "header": [{"key": "Content-Type", "value": "application/json"}],
                "body": {
                  "mode": "raw",
                  "raw": "{\n  \"employeeId\": \"{{employee_id}}\",\n  \"locationId\": \"{{location_id}}\",\n  \"balance\": 20\n}",
                  "options": {"raw": {"language": "json"}}
                },
                "url": {
                  "raw": "{{hcm_url}}/_control/refresh",
                  "host": ["{{hcm_url}}"],
                  "path": ["_control", "refresh"]
                },
                "description": "Seed HCM to 20."
              },
              "event": [
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": ["pm.test('Seed OK', () => pm.response.to.have.status(200));"]
                  }
                }
              ]
            },
            {
              "name": "D2 — Set scenario: correct",
              "request": {
                "method": "POST",
                "header": [{"key": "Content-Type", "value": "application/json"}],
                "body": {
                  "mode": "raw",
                  "raw": "{\n  \"scenario\": \"correct\"\n}",
                  "options": {"raw": {"language": "json"}}
                },
                "url": {
                  "raw": "{{hcm_url}}/_control/scenario",
                  "host": ["{{hcm_url}}"],
                  "path": ["_control", "scenario"]
                },
                "description": "Normal HCM behavior."
              },
              "event": [
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": ["pm.test('Scenario set OK', () => pm.response.to.have.status(200));"]
                  }
                }
              ]
            },
            {
              "name": "D3 — View balance (lazy-hydrate)",
              "request": {
                "method": "GET",
                "header": [
                  {"key": "X-Employee-Id", "value": "{{employee_id}}"},
                  {"key": "X-Role", "value": "employee"}
                ],
                "url": {
                  "raw": "{{base_url}}/balances?employeeId={{employee_id}}&locationId={{location_id}}",
                  "host": ["{{base_url}}"],
                  "path": ["balances"],
                  "query": [
                    {"key": "employeeId", "value": "{{employee_id}}"},
                    {"key": "locationId", "value": "{{location_id}}"}
                  ]
                },
                "description": "Warm up the balance row."
              },
              "event": [
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": ["pm.test('Status 200', () => pm.response.to.have.status(200));"]
                  }
                }
              ]
            },
            {
              "name": "D4 — Submit Jun 2–3 (key D1)",
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.collectionVariables.set('idempotency_key', 'd20-key-1-' + Date.now());"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Status 201', () => pm.response.to.have.status(201));",
                      "pm.test('First request PENDING', () => pm.expect(pm.response.json().status).to.eql('PENDING'));"
                    ]
                  }
                }
              ],
              "request": {
                "method": "POST",
                "header": [
                  {"key": "X-Employee-Id", "value": "{{employee_id}}"},
                  {"key": "X-Role", "value": "employee"},
                  {"key": "Idempotency-Key", "value": "{{idempotency_key}}"},
                  {"key": "Content-Type", "value": "application/json"}
                ],
                "body": {
                  "mode": "raw",
                  "raw": "{\n  \"employeeId\": \"{{employee_id}}\",\n  \"locationId\": \"{{location_id}}\",\n  \"startDate\": \"2026-06-02\",\n  \"endDate\": \"2026-06-03\"\n}",
                  "options": {"raw": {"language": "json"}}
                },
                "url": {
                  "raw": "{{base_url}}/time-off-requests",
                  "host": ["{{base_url}}"],
                  "path": ["time-off-requests"]
                },
                "description": "First submit: Jun 2–3. Expect PENDING."
              }
            },
            {
              "name": "D5 — Submit Jun 3–4 (key D2) → 409 overlap (E20)",
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "// Fresh key so this is NOT an idempotency replay — tests overlap guard.",
                      "pm.collectionVariables.set('idempotency_key', 'd20-key-2-' + Date.now());"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('409 Conflict — boundary overlap on shared Jun 3', () => pm.response.to.have.status(409));"
                    ]
                  }
                }
              ],
              "request": {
                "method": "POST",
                "header": [
                  {"key": "X-Employee-Id", "value": "{{employee_id}}"},
                  {"key": "X-Role", "value": "employee"},
                  {"key": "Idempotency-Key", "value": "{{idempotency_key}}"},
                  {"key": "Content-Type", "value": "application/json"}
                ],
                "body": {
                  "mode": "raw",
                  "raw": "{\n  \"employeeId\": \"{{employee_id}}\",\n  \"locationId\": \"{{location_id}}\",\n  \"startDate\": \"2026-06-03\",\n  \"endDate\": \"2026-06-04\"\n}",
                  "options": {"raw": {"language": "json"}}
                },
                "url": {
                  "raw": "{{base_url}}/time-off-requests",
                  "host": ["{{base_url}}"],
                  "path": ["time-off-requests"]
                },
                "description": "Jun 3–4 shares Jun 3 with the first request. Inclusive overlap predicate (newStart <= existingEnd AND newEnd >= existingStart) → 409. ADR-012 ②/E20."
              }
            }
          ]
        },
        {
          "name": "E — Cancel approved → REVERSE (E9)",
          "description": "## Goal\nVerify that cancelling an APPROVED request restores the local balance and the dispatcher sends a REVERSE to HCM, which credits the days back (E9, FR-6, ADR-004/008).\n\n## Expected flow\n1–7: Replay scenario A (seed, scenario, hydrate, submit, view, approve, wait 6 s).\n8. Cancel the approved request → CANCELLED, available restored.\n9. Wait 6 s for the REVERSE dispatcher tick.\n10. Check HCM balance directly → back to 20.\n\n## What to verify\n- Step 8: status=CANCELLED.\n- Step 10: HCM balance=20 (REVERSE applied).",
          "item": [
            {
              "name": "E-A1 — Seed HCM balance (20)",
              "request": {
                "method": "POST",
                "header": [{"key": "Content-Type", "value": "application/json"}],
                "body": {
                  "mode": "raw",
                  "raw": "{\n  \"employeeId\": \"{{employee_id}}\",\n  \"locationId\": \"{{location_id}}\",\n  \"balance\": 20\n}",
                  "options": {"raw": {"language": "json"}}
                },
                "url": {
                  "raw": "{{hcm_url}}/_control/refresh",
                  "host": ["{{hcm_url}}"],
                  "path": ["_control", "refresh"]
                },
                "description": "Seed HCM to 20."
              },
              "event": [
                {"listen": "test", "script": {"type": "text/javascript", "exec": ["pm.test('Seed OK', () => pm.response.to.have.status(200));"]}}
              ]
            },
            {
              "name": "E-A2 — Set scenario: correct",
              "request": {
                "method": "POST",
                "header": [{"key": "Content-Type", "value": "application/json"}],
                "body": {"mode": "raw", "raw": "{\n  \"scenario\": \"correct\"\n}", "options": {"raw": {"language": "json"}}},
                "url": {"raw": "{{hcm_url}}/_control/scenario", "host": ["{{hcm_url}}"], "path": ["_control", "scenario"]},
                "description": "Normal HCM behavior."
              },
              "event": [
                {"listen": "test", "script": {"type": "text/javascript", "exec": ["pm.test('OK', () => pm.response.to.have.status(200));"]}}
              ]
            },
            {
              "name": "E-A3 — View balance (lazy-hydrate)",
              "request": {
                "method": "GET",
                "header": [{"key": "X-Employee-Id", "value": "{{employee_id}}"}, {"key": "X-Role", "value": "employee"}],
                "url": {
                  "raw": "{{base_url}}/balances?employeeId={{employee_id}}&locationId={{location_id}}",
                  "host": ["{{base_url}}"], "path": ["balances"],
                  "query": [{"key": "employeeId", "value": "{{employee_id}}"}, {"key": "locationId", "value": "{{location_id}}"}]
                },
                "description": "Warm up balance via ADR-014."
              },
              "event": [
                {"listen": "test", "script": {"type": "text/javascript", "exec": ["pm.test('Status 200', () => pm.response.to.have.status(200));"]}}
              ]
            },
            {
              "name": "E-A4 — Submit (Jun 1–2)",
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const uuid = (typeof crypto !== 'undefined' && crypto.randomUUID) ? crypto.randomUUID() : 'idem-' + Date.now();",
                      "pm.collectionVariables.set('idempotency_key', uuid);"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('201 PENDING', () => { pm.response.to.have.status(201); pm.expect(pm.response.json().status).to.eql('PENDING'); });",
                      "pm.collectionVariables.set('request_id', pm.response.json().id);"
                    ]
                  }
                }
              ],
              "request": {
                "method": "POST",
                "header": [
                  {"key": "X-Employee-Id", "value": "{{employee_id}}"}, {"key": "X-Role", "value": "employee"},
                  {"key": "Idempotency-Key", "value": "{{idempotency_key}}"}, {"key": "Content-Type", "value": "application/json"}
                ],
                "body": {
                  "mode": "raw",
                  "raw": "{\n  \"employeeId\": \"{{employee_id}}\",\n  \"locationId\": \"{{location_id}}\",\n  \"startDate\": \"2026-06-01\",\n  \"endDate\": \"2026-06-02\"\n}",
                  "options": {"raw": {"language": "json"}}
                },
                "url": {"raw": "{{base_url}}/time-off-requests", "host": ["{{base_url}}"], "path": ["time-off-requests"]},
                "description": "Submit 2 days. Captures request_id."
              }
            },
            {
              "name": "E-A5 — Approve request",
              "request": {
                "method": "POST",
                "header": [{"key": "X-Employee-Id", "value": "{{manager_id}}"}, {"key": "X-Role", "value": "manager"}, {"key": "Content-Type", "value": "application/json"}],
                "body": {"mode": "raw", "raw": "{}", "options": {"raw": {"language": "json"}}},
                "url": {"raw": "{{base_url}}/time-off-requests/{{request_id}}/approve", "host": ["{{base_url}}"], "path": ["time-off-requests", "{{request_id}}", "approve"]},
                "description": "Approve → PENDING_SYNC."
              },
              "event": [
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('200/201', () => pm.expect([200,201]).to.include(pm.response.code));",
                      "pm.test('PENDING_SYNC or APPROVED', () => pm.expect(['PENDING_SYNC','APPROVED']).to.include(pm.response.json().status));"
                    ]
                  }
                }
              ]
            },
            {
              "name": "⏱ Wait 6s (dispatcher tick — promote to APPROVED)",
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": ["const now = Date.now(); while (Date.now() - now < 6000) { /* busy-wait */ }"]
                  }
                },
                {
                  "listen": "test",
                  "script": {"type": "text/javascript", "exec": ["pm.test('Wait OK', () => pm.response.to.have.status(200));"]}
                }
              ],
              "request": {
                "method": "GET", "header": [],
                "url": {"raw": "{{base_url}}/", "host": ["{{base_url}}"], "path": [""]},
                "description": "Wait for dispatcher to promote PENDING_SYNC → APPROVED."
              }
            },
            {
              "name": "E1 — Cancel approved request",
              "request": {
                "method": "POST",
                "header": [
                  {"key": "X-Employee-Id", "value": "{{employee_id}}"}, {"key": "X-Role", "value": "employee"},
                  {"key": "Content-Type", "value": "application/json"}
                ],
                "body": {"mode": "raw", "raw": "{}", "options": {"raw": {"language": "json"}}},
                "url": {
                  "raw": "{{base_url}}/time-off-requests/{{request_id}}/cancel",
                  "host": ["{{base_url}}"], "path": ["time-off-requests", "{{request_id}}", "cancel"]
                },
                "description": "Cancel APPROVED → CANCELLED. Balance restored (available += 2). REVERSE enqueued in outbox."
              },
              "event": [
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('200/201', () => pm.expect([200,201]).to.include(pm.response.code));",
                      "pm.test('Status=CANCELLED', () => pm.expect(pm.response.json().status).to.eql('CANCELLED'));"
                    ]
                  }
                }
              ]
            },
            {
              "name": "⏱ Wait 6s (dispatcher tick — send REVERSE)",
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": ["const now = Date.now(); while (Date.now() - now < 6000) { /* busy-wait */ }"]
                  }
                },
                {
                  "listen": "test",
                  "script": {"type": "text/javascript", "exec": ["pm.test('Wait OK', () => pm.response.to.have.status(200));"]}
                }
              ],
              "request": {
                "method": "GET", "header": [],
                "url": {"raw": "{{base_url}}/", "host": ["{{base_url}}"], "path": [""]},
                "description": "Wait for dispatcher to send the REVERSE outbox row to HCM."
              }
            },
            {
              "name": "E2 — Direct: verify HCM balance=20 (REVERSE applied)",
              "request": {
                "method": "GET",
                "header": [],
                "url": {
                  "raw": "{{hcm_url}}/hcm/balance?employeeId={{employee_id}}&locationId={{location_id}}",
                  "host": ["{{hcm_url}}"], "path": ["hcm", "balance"],
                  "query": [{"key": "employeeId", "value": "{{employee_id}}"}, {"key": "locationId", "value": "{{location_id}}"}]
                },
                "description": "REVERSE should have credited the 2 days back at HCM. Expect balance=20."
              },
              "event": [
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Status 200', () => pm.response.to.have.status(200));",
                      "pm.test('HCM balance restored to 20', () => pm.expect(pm.response.json().balance).to.eql(20));"
                    ]
                  }
                }
              ]
            }
          ]
        },
        {
          "name": "F — Reconcile anniversary bonus (E7)",
          "description": "## Goal\nVerify a batch corpus with a higher balance (anniversary bonus) increases the local available without disturbing active reservations (E7, ADR-003/009).\n\n## Expected flow\n1. Lazy-hydrate local balance (available=20, reserved=0 baseline).\n2. Refresh HCM to 30 (bonus added).\n3. Ingest batch with sequence > last and balance=30.\n4. View balance → available=30, reserved unchanged.\n\n## What to verify\n- Step 4: available=30 (bonus applied), reserved=0 if no active requests.",
          "item": [
            {
              "name": "F1 — Seed HCM balance (20) and lazy-hydrate",
              "request": {
                "method": "POST",
                "header": [{"key": "Content-Type", "value": "application/json"}],
                "body": {
                  "mode": "raw",
                  "raw": "{\n  \"employeeId\": \"{{employee_id}}\",\n  \"locationId\": \"{{location_id}}\",\n  \"balance\": 20\n}",
                  "options": {"raw": {"language": "json"}}
                },
                "url": {"raw": "{{hcm_url}}/_control/refresh", "host": ["{{hcm_url}}"], "path": ["_control", "refresh"]},
                "description": "Seed initial balance."
              },
              "event": [
                {"listen": "test", "script": {"type": "text/javascript", "exec": ["pm.test('Seed OK', () => pm.response.to.have.status(200));"]}}
              ]
            },
            {
              "name": "F2 — View balance (lazy-hydrate local row)",
              "request": {
                "method": "GET",
                "header": [{"key": "X-Employee-Id", "value": "{{employee_id}}"}, {"key": "X-Role", "value": "employee"}],
                "url": {
                  "raw": "{{base_url}}/balances?employeeId={{employee_id}}&locationId={{location_id}}",
                  "host": ["{{base_url}}"], "path": ["balances"],
                  "query": [{"key": "employeeId", "value": "{{employee_id}}"}, {"key": "locationId", "value": "{{location_id}}"}]
                },
                "description": "Warm up local balance row via ADR-014 lazy-hydrate."
              },
              "event": [
                {"listen": "test", "script": {"type": "text/javascript", "exec": ["pm.test('Status 200', () => pm.response.to.have.status(200));"]}}
              ]
            },
            {
              "name": "F3 — Refresh HCM balance to 30 (anniversary bonus)",
              "request": {
                "method": "POST",
                "header": [{"key": "Content-Type", "value": "application/json"}],
                "body": {
                  "mode": "raw",
                  "raw": "{\n  \"employeeId\": \"{{employee_id}}\",\n  \"locationId\": \"{{location_id}}\",\n  \"balance\": 30\n}",
                  "options": {"raw": {"language": "json"}}
                },
                "url": {"raw": "{{hcm_url}}/_control/refresh", "host": ["{{hcm_url}}"], "path": ["_control", "refresh"]},
                "description": "Simulate an anniversary bonus: HCM-side balance jumps to 30."
              },
              "event": [
                {"listen": "test", "script": {"type": "text/javascript", "exec": ["pm.test('Bonus seeded OK', () => pm.response.to.have.status(200));"]}}
              ]
            },
            {
              "name": "F4 — Ingest batch (sequence=100, balance=30)",
              "request": {
                "method": "POST",
                "header": [{"key": "Content-Type", "value": "application/json"}],
                "body": {
                  "mode": "raw",
                  "raw": "{\n  \"sequence\": 100,\n  \"asOf\": \"2026-06-15T00:00:00.000Z\",\n  \"balances\": [\n    {\n      \"employeeId\": \"{{employee_id}}\",\n      \"locationId\": \"{{location_id}}\",\n      \"balance\": 30,\n      \"asOf\": \"2026-06-15T00:00:00.000Z\"\n    }\n  ]\n}",
                  "options": {"raw": {"language": "json"}}
                },
                "url": {"raw": "{{base_url}}/timeoff/hcm/batch", "host": ["{{base_url}}"], "path": ["timeoff", "hcm", "batch"]},
                "description": "Ingest the bonus batch. sequence=100 is safely above any prior run. ADR-009 sequence guard passes; ADR-003 reconcile sets base=30."
              },
              "event": [
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": ["pm.test('Batch accepted', () => pm.expect([200,201,202]).to.include(pm.response.code));"]
                  }
                }
              ]
            },
            {
              "name": "F5 — View balance (available=30)",
              "request": {
                "method": "GET",
                "header": [{"key": "X-Employee-Id", "value": "{{employee_id}}"}, {"key": "X-Role", "value": "employee"}],
                "url": {
                  "raw": "{{base_url}}/balances?employeeId={{employee_id}}&locationId={{location_id}}",
                  "host": ["{{base_url}}"], "path": ["balances"],
                  "query": [{"key": "employeeId", "value": "{{employee_id}}"}, {"key": "locationId", "value": "{{location_id}}"}]
                },
                "description": "Post-reconcile: base=30, no unacked effects to replay → available=30."
              },
              "event": [
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Status 200', () => pm.response.to.have.status(200));",
                      "pm.test('available=30 (bonus applied)', () => pm.expect(pm.response.json().available).to.eql(30));"
                    ]
                  }
                }
              ]
            }
          ]
        },
        {
          "name": "G — HCM timeout at approve → retry-cap → REJECTED (E4/E5)",
          "description": "## Goal\nExercise the full retry-cap compensation path: HCM stays down through all 5 attempts, triggering REVERSE and REJECTED (E4→E5, ADR-004).\n\n## Expected flow\n1. Seed 20, correct, hydrate, submit.\n2. Switch HCM to `timeout`.\n3. Approve → PENDING_SYNC.\n4–8. Wait 6 s × 5 rounds for 5 dispatcher failures.\n9. View balance → available=20, reserved=0 (restore fired on retry-cap).\n\n## What to verify\n- After 5× 6 s waits: status=REJECTED (retry-cap fired).\n- Balance restored: available=20, reserved=0.",
          "item": [
            {
              "name": "G1 — Seed HCM balance (20)",
              "request": {
                "method": "POST",
                "header": [{"key": "Content-Type", "value": "application/json"}],
                "body": {
                  "mode": "raw",
                  "raw": "{\n  \"employeeId\": \"{{employee_id}}\",\n  \"locationId\": \"{{location_id}}\",\n  \"balance\": 20\n}",
                  "options": {"raw": {"language": "json"}}
                },
                "url": {"raw": "{{hcm_url}}/_control/refresh", "host": ["{{hcm_url}}"], "path": ["_control", "refresh"]},
                "description": "Seed to 20."
              },
              "event": [
                {"listen": "test", "script": {"type": "text/javascript", "exec": ["pm.test('Seed OK', () => pm.response.to.have.status(200));"]}}
              ]
            },
            {
              "name": "G2 — Set scenario: correct (for approve)",
              "request": {
                "method": "POST",
                "header": [{"key": "Content-Type", "value": "application/json"}],
                "body": {"mode": "raw", "raw": "{\n  \"scenario\": \"correct\"\n}", "options": {"raw": {"language": "json"}}},
                "url": {"raw": "{{hcm_url}}/_control/scenario", "host": ["{{hcm_url}}"], "path": ["_control", "scenario"]},
                "description": "Start with correct so approve's HCM read succeeds."
              },
              "event": [
                {"listen": "test", "script": {"type": "text/javascript", "exec": ["pm.test('OK', () => pm.response.to.have.status(200));"]}}
              ]
            },
            {
              "name": "G3 — View balance (lazy-hydrate)",
              "request": {
                "method": "GET",
                "header": [{"key": "X-Employee-Id", "value": "{{employee_id}}"}, {"key": "X-Role", "value": "employee"}],
                "url": {
                  "raw": "{{base_url}}/balances?employeeId={{employee_id}}&locationId={{location_id}}",
                  "host": ["{{base_url}}"], "path": ["balances"],
                  "query": [{"key": "employeeId", "value": "{{employee_id}}"}, {"key": "locationId", "value": "{{location_id}}"}]
                },
                "description": "Warm up balance."
              },
              "event": [
                {"listen": "test", "script": {"type": "text/javascript", "exec": ["pm.test('Status 200', () => pm.response.to.have.status(200));"]}}
              ]
            },
            {
              "name": "G4 — Submit (Jun 1–2)",
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const uuid = (typeof crypto !== 'undefined' && crypto.randomUUID) ? crypto.randomUUID() : 'idem-' + Date.now();",
                      "pm.collectionVariables.set('idempotency_key', uuid);"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('201 PENDING', () => pm.response.to.have.status(201));",
                      "pm.collectionVariables.set('request_id', pm.response.json().id);"
                    ]
                  }
                }
              ],
              "request": {
                "method": "POST",
                "header": [
                  {"key": "X-Employee-Id", "value": "{{employee_id}}"}, {"key": "X-Role", "value": "employee"},
                  {"key": "Idempotency-Key", "value": "{{idempotency_key}}"}, {"key": "Content-Type", "value": "application/json"}
                ],
                "body": {
                  "mode": "raw",
                  "raw": "{\n  \"employeeId\": \"{{employee_id}}\",\n  \"locationId\": \"{{location_id}}\",\n  \"startDate\": \"2026-06-01\",\n  \"endDate\": \"2026-06-02\"\n}",
                  "options": {"raw": {"language": "json"}}
                },
                "url": {"raw": "{{base_url}}/time-off-requests", "host": ["{{base_url}}"], "path": ["time-off-requests"]},
                "description": "Submit 2 days. Saves request_id."
              }
            },
            {
              "name": "G5 — Set scenario: timeout (HCM goes down)",
              "request": {
                "method": "POST",
                "header": [{"key": "Content-Type", "value": "application/json"}],
                "body": {"mode": "raw", "raw": "{\n  \"scenario\": \"timeout\"\n}", "options": {"raw": {"language": "json"}}},
                "url": {"raw": "{{hcm_url}}/_control/scenario", "host": ["{{hcm_url}}"], "path": ["_control", "scenario"]},
                "description": "Switch to timeout BEFORE approval so the dispatcher's FILE calls all fail."
              },
              "event": [
                {"listen": "test", "script": {"type": "text/javascript", "exec": ["pm.test('Timeout scenario set', () => pm.response.to.have.status(200));"]}}
              ]
            },
            {
              "name": "G6 — Approve → PENDING_SYNC",
              "request": {
                "method": "POST",
                "header": [{"key": "X-Employee-Id", "value": "{{manager_id}}"}, {"key": "X-Role", "value": "manager"}, {"key": "Content-Type", "value": "application/json"}],
                "body": {"mode": "raw", "raw": "{}", "options": {"raw": {"language": "json"}}},
                "url": {"raw": "{{base_url}}/time-off-requests/{{request_id}}/approve", "host": ["{{base_url}}"], "path": ["time-off-requests", "{{request_id}}", "approve"]},
                "description": "Approve with HCM in timeout. Outbox FILE enqueued; status=PENDING_SYNC."
              },
              "event": [
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('200/201', () => pm.expect([200,201]).to.include(pm.response.code));",
                      "pm.test('PENDING_SYNC (HCM down at approve time)', () => pm.expect(['PENDING_SYNC','APPROVED']).to.include(pm.response.json().status));"
                    ]
                  }
                }
              ]
            },
            {
              "name": "⏱ Wait 6s — dispatcher attempt 1 (fails)",
              "event": [
                {"listen": "prerequest", "script": {"type": "text/javascript", "exec": ["const now = Date.now(); while (Date.now() - now < 6000) { /* busy-wait */ }"]}},
                {"listen": "test", "script": {"type": "text/javascript", "exec": ["pm.test('Wait OK', () => pm.response.to.have.status(200));"]}}
              ],
              "request": {"method": "GET", "header": [], "url": {"raw": "{{base_url}}/", "host": ["{{base_url}}"], "path": [""]}, "description": "Dispatcher fires, FILE call fails (timeout). Attempt count = 1."}
            },
            {
              "name": "⏱ Wait 6s — dispatcher attempt 2 (fails)",
              "event": [
                {"listen": "prerequest", "script": {"type": "text/javascript", "exec": ["const now = Date.now(); while (Date.now() - now < 6000) { /* busy-wait */ }"]}},
                {"listen": "test", "script": {"type": "text/javascript", "exec": ["pm.test('Wait OK', () => pm.response.to.have.status(200));"]}}
              ],
              "request": {"method": "GET", "header": [], "url": {"raw": "{{base_url}}/", "host": ["{{base_url}}"], "path": [""]}, "description": "Attempt 2 fails."}
            },
            {
              "name": "⏱ Wait 6s — dispatcher attempt 3 (fails)",
              "event": [
                {"listen": "prerequest", "script": {"type": "text/javascript", "exec": ["const now = Date.now(); while (Date.now() - now < 6000) { /* busy-wait */ }"]}},
                {"listen": "test", "script": {"type": "text/javascript", "exec": ["pm.test('Wait OK', () => pm.response.to.have.status(200));"]}}
              ],
              "request": {"method": "GET", "header": [], "url": {"raw": "{{base_url}}/", "host": ["{{base_url}}"], "path": [""]}, "description": "Attempt 3 fails."}
            },
            {
              "name": "⏱ Wait 6s — dispatcher attempt 4 (fails)",
              "event": [
                {"listen": "prerequest", "script": {"type": "text/javascript", "exec": ["const now = Date.now(); while (Date.now() - now < 6000) { /* busy-wait */ }"]}},
                {"listen": "test", "script": {"type": "text/javascript", "exec": ["pm.test('Wait OK', () => pm.response.to.have.status(200));"]}}
              ],
              "request": {"method": "GET", "header": [], "url": {"raw": "{{base_url}}/", "host": ["{{base_url}}"], "path": [""]}, "description": "Attempt 4 fails."}
            },
            {
              "name": "⏱ Wait 6s — dispatcher attempt 5 → retry-cap fires → REJECTED",
              "event": [
                {"listen": "prerequest", "script": {"type": "text/javascript", "exec": ["const now = Date.now(); while (Date.now() - now < 6000) { /* busy-wait */ }"]}},
                {"listen": "test", "script": {"type": "text/javascript", "exec": ["pm.test('Wait OK', () => pm.response.to.have.status(200));"]}}
              ],
              "request": {"method": "GET", "header": [], "url": {"raw": "{{base_url}}/", "host": ["{{base_url}}"], "path": [""]}, "description": "5th attempt hits retry cap. Dispatcher: outbox FILE → FAILED, restore balance, status → REJECTED, REVERSE enqueued."}
            },
            {
              "name": "G7 — View balance (available=20, reserved=0 — restored)",
              "request": {
                "method": "GET",
                "header": [{"key": "X-Employee-Id", "value": "{{employee_id}}"}, {"key": "X-Role", "value": "employee"}],
                "url": {
                  "raw": "{{base_url}}/balances?employeeId={{employee_id}}&locationId={{location_id}}",
                  "host": ["{{base_url}}"], "path": ["balances"],
                  "query": [{"key": "employeeId", "value": "{{employee_id}}"}, {"key": "locationId", "value": "{{location_id}}"}]
                },
                "description": "After retry-cap: restore() fired → available=20, reserved=0."
              },
              "event": [
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Status 200', () => pm.response.to.have.status(200));",
                      "pm.test('available=20, reserved=0 (balance restored)', () => {",
                      "  const j = pm.response.json();",
                      "  pm.expect(j.available).to.eql(20);",
                      "  pm.expect(j.reserved).to.eql(0);",
                      "});"
                    ]
                  }
                }
              ]
            }
          ]
        },
        {
          "name": "H — Reconcile drives balance negative → needsReview (E26)",
          "description": "## Goal\nVerify that a batch reconcile that would make `available` negative instead sets `needsReview=true` and emits `FLAGGED_NEGATIVE`, and that a manager can clear it (E26, B4, ADR-003).\n\n## Expected flow\n1. Run scenario A steps 1–7 (seed 20, correct, hydrate, submit, approve, wait) so a request is APPROVED and HCM=18.\n2. Refresh HCM to 1 (simulating a drastic year-start drop).\n3. Ingest batch with balance=1 and a fresh sequence.\n4. Reconcile: base=1, minus 2 (unacked APPROVED) = -1 → needsReview=true.\n5. View balance → needsReview=true.\n6. Resolve review (manager) → needsReview cleared.\n\n## What to verify\n- Step 5: needsReview=true.\n- Step 6: 200/204 with needsReview cleared on next view.",
          "item": [
            {
              "name": "H-A1 — Seed HCM (20), scenario correct",
              "request": {
                "method": "POST",
                "header": [{"key": "Content-Type", "value": "application/json"}],
                "body": {"mode": "raw", "raw": "{\n  \"employeeId\": \"{{employee_id}}\",\n  \"locationId\": \"{{location_id}}\",\n  \"balance\": 20\n}", "options": {"raw": {"language": "json"}}},
                "url": {"raw": "{{hcm_url}}/_control/refresh", "host": ["{{hcm_url}}"], "path": ["_control", "refresh"]},
                "description": "Seed HCM to 20."
              },
              "event": [
                {"listen": "test", "script": {"type": "text/javascript", "exec": ["pm.test('Seed OK', () => pm.response.to.have.status(200));"]}}
              ]
            },
            {
              "name": "H-A2 — Set scenario: correct",
              "request": {
                "method": "POST",
                "header": [{"key": "Content-Type", "value": "application/json"}],
                "body": {"mode": "raw", "raw": "{\n  \"scenario\": \"correct\"\n}", "options": {"raw": {"language": "json"}}},
                "url": {"raw": "{{hcm_url}}/_control/scenario", "host": ["{{hcm_url}}"], "path": ["_control", "scenario"]},
                "description": "Normal HCM."
              },
              "event": [
                {"listen": "test", "script": {"type": "text/javascript", "exec": ["pm.test('OK', () => pm.response.to.have.status(200));"]}}
              ]
            },
            {
              "name": "H-A3 — View balance (lazy-hydrate)",
              "request": {
                "method": "GET",
                "header": [{"key": "X-Employee-Id", "value": "{{employee_id}}"}, {"key": "X-Role", "value": "employee"}],
                "url": {
                  "raw": "{{base_url}}/balances?employeeId={{employee_id}}&locationId={{location_id}}",
                  "host": ["{{base_url}}"], "path": ["balances"],
                  "query": [{"key": "employeeId", "value": "{{employee_id}}"}, {"key": "locationId", "value": "{{location_id}}"}]
                },
                "description": "Warm up local balance."
              },
              "event": [
                {"listen": "test", "script": {"type": "text/javascript", "exec": ["pm.test('Status 200', () => pm.response.to.have.status(200));"]}}
              ]
            },
            {
              "name": "H-A4 — Submit (Jun 1–2)",
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const uuid = (typeof crypto !== 'undefined' && crypto.randomUUID) ? crypto.randomUUID() : 'idem-' + Date.now();",
                      "pm.collectionVariables.set('idempotency_key', uuid);"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('201 PENDING', () => pm.response.to.have.status(201));",
                      "pm.collectionVariables.set('request_id', pm.response.json().id);"
                    ]
                  }
                }
              ],
              "request": {
                "method": "POST",
                "header": [
                  {"key": "X-Employee-Id", "value": "{{employee_id}}"}, {"key": "X-Role", "value": "employee"},
                  {"key": "Idempotency-Key", "value": "{{idempotency_key}}"}, {"key": "Content-Type", "value": "application/json"}
                ],
                "body": {
                  "mode": "raw",
                  "raw": "{\n  \"employeeId\": \"{{employee_id}}\",\n  \"locationId\": \"{{location_id}}\",\n  \"startDate\": \"2026-06-01\",\n  \"endDate\": \"2026-06-02\"\n}",
                  "options": {"raw": {"language": "json"}}
                },
                "url": {"raw": "{{base_url}}/time-off-requests", "host": ["{{base_url}}"], "path": ["time-off-requests"]},
                "description": "Submit 2 days."
              }
            },
            {
              "name": "H-A5 — Approve request",
              "request": {
                "method": "POST",
                "header": [{"key": "X-Employee-Id", "value": "{{manager_id}}"}, {"key": "X-Role", "value": "manager"}, {"key": "Content-Type", "value": "application/json"}],
                "body": {"mode": "raw", "raw": "{}", "options": {"raw": {"language": "json"}}},
                "url": {"raw": "{{base_url}}/time-off-requests/{{request_id}}/approve", "host": ["{{base_url}}"], "path": ["time-off-requests", "{{request_id}}", "approve"]},
                "description": "Approve → PENDING_SYNC."
              },
              "event": [
                {
                  "listen": "test",
                  "script": {"type": "text/javascript", "exec": ["pm.test('200/201', () => pm.expect([200,201]).to.include(pm.response.code));"]}
                }
              ]
            },
            {
              "name": "⏱ Wait 6s (dispatcher tick — promote to APPROVED)",
              "event": [
                {"listen": "prerequest", "script": {"type": "text/javascript", "exec": ["const now = Date.now(); while (Date.now() - now < 6000) { /* busy-wait */ }"]}},
                {"listen": "test", "script": {"type": "text/javascript", "exec": ["pm.test('Wait OK', () => pm.response.to.have.status(200));"]}}
              ],
              "request": {"method": "GET", "header": [], "url": {"raw": "{{base_url}}/", "host": ["{{base_url}}"], "path": [""]}, "description": "Wait for APPROVED."}
            },
            {
              "name": "H1 — Refresh HCM to 1 (year-start drop)",
              "request": {
                "method": "POST",
                "header": [{"key": "Content-Type", "value": "application/json"}],
                "body": {
                  "mode": "raw",
                  "raw": "{\n  \"employeeId\": \"{{employee_id}}\",\n  \"locationId\": \"{{location_id}}\",\n  \"balance\": 1\n}",
                  "options": {"raw": {"language": "json"}}
                },
                "url": {"raw": "{{hcm_url}}/_control/refresh", "host": ["{{hcm_url}}"], "path": ["_control", "refresh"]},
                "description": "Simulate a drastic year-start balance drop — HCM now says only 1 day available, but we have a 2-day APPROVED that HCM may not have acked yet."
              },
              "event": [
                {"listen": "test", "script": {"type": "text/javascript", "exec": ["pm.test('Drop seeded OK', () => pm.response.to.have.status(200));"]}}
              ]
            },
            {
              "name": "H2 — Ingest batch (sequence=200, balance=1)",
              "request": {
                "method": "POST",
                "header": [{"key": "Content-Type", "value": "application/json"}],
                "body": {
                  "mode": "raw",
                  "raw": "{\n  \"sequence\": 200,\n  \"asOf\": \"2026-07-01T00:00:00.000Z\",\n  \"balances\": [\n    {\n      \"employeeId\": \"{{employee_id}}\",\n      \"locationId\": \"{{location_id}}\",\n      \"balance\": 1,\n      \"asOf\": \"2026-07-01T00:00:00.000Z\"\n    }\n  ]\n}",
                  "options": {"raw": {"language": "json"}}
                },
                "url": {"raw": "{{base_url}}/timeoff/hcm/batch", "host": ["{{base_url}}"], "path": ["timeoff", "hcm", "batch"]},
                "description": "sequence=200 clears any prior batch guard. ADR-003 reconcile: base=1, minus 2 (APPROVED with hcmAckAt>asOf) = -1 → needsReview=true."
              },
              "event": [
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": ["pm.test('Batch accepted', () => pm.expect([200,201,202]).to.include(pm.response.code));"]
                  }
                }
              ]
            },
            {
              "name": "H3 — View balance (needsReview=true)",
              "request": {
                "method": "GET",
                "header": [{"key": "X-Employee-Id", "value": "{{employee_id}}"}, {"key": "X-Role", "value": "employee"}],
                "url": {
                  "raw": "{{base_url}}/balances?employeeId={{employee_id}}&locationId={{location_id}}",
                  "host": ["{{base_url}}"], "path": ["balances"],
                  "query": [{"key": "employeeId", "value": "{{employee_id}}"}, {"key": "locationId", "value": "{{location_id}}"}]
                },
                "description": "Reconcile drove available negative → needsReview=true. FLAGGED_NEGATIVE ReconciliationEvent emitted."
              },
              "event": [
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Status 200', () => pm.response.to.have.status(200));",
                      "pm.test('needsReview=true', () => pm.expect(pm.response.json().needsReview).to.eql(true));"
                    ]
                  }
                }
              ]
            },
            {
              "name": "H4 — Resolve review (manager clears needsReview)",
              "request": {
                "method": "PATCH",
                "header": [
                  {"key": "X-Employee-Id", "value": "{{manager_id}}"}, {"key": "X-Role", "value": "manager"},
                  {"key": "Content-Type", "value": "application/json"}
                ],
                "body": {
                  "mode": "raw",
                  "raw": "{\n  \"employeeId\": \"{{employee_id}}\",\n  \"locationId\": \"{{location_id}}\"\n}",
                  "options": {"raw": {"language": "json"}}
                },
                "url": {"raw": "{{base_url}}/balances/resolve-review", "host": ["{{base_url}}"], "path": ["balances", "resolve-review"]},
                "description": "Manager acknowledges the negative reconcile and clears the flag. needsReview → false."
              },
              "event": [
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": ["pm.test('Resolve OK', () => pm.expect([200,204]).to.include(pm.response.code));"]
                  }
                }
              ]
            }
          ]
        },
        {
          "name": "I — TTL expiry note (E15 — test-only)",
          "description": "## This scenario cannot run end-to-end in Postman\n\nThe reservation reaper runs on a `@Cron(EVERY_5_MINUTES)` schedule and the default TTL is 14 days (`RESERVATION_TTL_DAYS`). A real Postman run-to-completion would require either:\n- Waiting 14+ days (impractical), or\n- Manually editing `RESERVATION_TTL_DAYS` in `compose.yml` to a very small value, restarting the stack, submitting a request, and waiting ~5 minutes for the next cron tick.\n\n**Recommended verification:** Use the Jest integration test `src/__tests__/cancel-and-reaper.spec.ts`, which injects a `FakeClock`, advances it 15 days (`clock.advance(15 * 24 * 60 * 60 * 1000)`), calls `reaper.sweep()` directly, and asserts:\n- Request status → `EXPIRED`\n- `reserved` released on the balance\n- Pending outbox FILE row voided in the same transaction (ADR-002/B2)\n\nRun with: `npm run test`\n\nThe placeholder request below is a health ping — it confirms the app is still up but does not exercise the reaper path.",
          "item": [
            {
              "name": "I — App health (TTL scenario: see folder description)",
              "request": {
                "method": "GET",
                "header": [],
                "url": {"raw": "{{base_url}}/", "host": ["{{base_url}}"], "path": [""]},
                "description": "Placeholder. The TTL/reaper scenario is best verified via `src/__tests__/cancel-and-reaper.spec.ts`. See the folder description for the FakeClock-based approach."
              },
              "event": [
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('App is up', () => pm.response.to.have.status(200));",
                      "pm.test('TTL/reaper scenario — see Jest test cancel-and-reaper.spec.ts for deterministic verification', () => pm.expect(true).to.be.true);"
                    ]
                  }
                }
              ]
            }
          ]
        }
      ]
    }
  ],
  "variable": [
    {"key": "base_url", "value": "http://localhost:3000", "type": "string"},
    {"key": "hcm_url", "value": "http://localhost:3101", "type": "string"},
    {"key": "employee_id", "value": "emp1", "type": "string"},
    {"key": "location_id", "value": "loc1", "type": "string"},
    {"key": "manager_id", "value": "manager1", "type": "string"},
    {"key": "request_id", "value": "", "type": "string", "description": "Auto-populated by the Submit request's test script."},
    {"key": "idempotency_key", "value": "", "type": "string", "description": "Auto-generated per Submit by the pre-request script. Set `reuse_idempotency_key=true` to replay the same key."},
    {"key": "reuse_idempotency_key", "value": "false", "type": "string", "description": "When true, Submit's pre-request script keeps the existing idempotency_key instead of minting a fresh one."},
    {"key": "idempotency_key_replay", "value": "", "type": "string", "description": "Fixed idempotency key used by scenario C (E8 idempotent replay). Set by the scenario pre-request script to a stable value so both submits use the same key."}
  ]
}
